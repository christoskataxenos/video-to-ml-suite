services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - polymind-data:/app/data
    restart: unless-stopped

  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend
    restart: unless-stopped

volumes:
  polymind-data:
